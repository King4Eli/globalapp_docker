name: global_apps

services:
  registry:
    image: registry:2
    restart: unless-stopped
    ports:
      - "10.50.0.1:9621:5000"
    volumes:
      - registry_data:/var/lib/registry

volumes:
  registry_data: