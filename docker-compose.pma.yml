name: global_apps

services:
  globalapp_pma:
    image: phpmyadmin/phpmyadmin
    container_name: globalapp_pma
    ports:
      - "11300:80"
    env_file:
      - .env/pma.env
    restart: unless-stopped