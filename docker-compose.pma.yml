name: global_apps

services:
  globalapp_pma:
    image: phpmyadmin/phpmyadmin
    container_name: globalapp_pma
    ports:
      - "10.50.0.1:101:80"
    env_file:
      - .env/pma.env
    restart: unless-stopped