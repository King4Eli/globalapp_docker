name: global_apps

services:
  global_pma:
    image: phpmyadmin/phpmyadmin
    container_name: global_pma
    ports:
      - "10.50.0.1:3307:80"
    env_file:
      - .env/pma.env
    restart: unless-stopped