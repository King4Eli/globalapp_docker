name: globalapp

services:
  mysql:
    image: mysql:8.0
    container_name: mysql
    env_file:
      - .env/mysql.env
    volumes:
      - mysql_data:/var/lib/mysql
    restart: unless-stopped
    ports:
      - "3306:3306"
volumes:
  mysql_data:
